# MagicCommander 更新日志

本文件为发布说明的单一事实来源（`npm run sync-version -- --release-notes` 自动抽取当前版本段）。

## [4.8.0] - 2026-09-02

### 新增

- 4.8 互操作与交付强化。
- 项目包往返强化：导出可移植项目包（manifest + 每文件 sha256 + 身份溯源）+ 按身份导入（同 projectId skip/update/new，保留 allocator_state）。
- 导入导出格式增强：项目历史快照导出为文件 + 导入合并去重；各格式（Excel/JSON/ZIP/快照/报告）可往返。
- 跨端资产互灌：设备库/技能库/模板可移植导入导出（schema 标识，合并去重 + 冲突提示）。
- 交付物清单与校验：渲染批次 manifest.json（逐文件 sha256 + 统计 + render_hash）+ 批次完整性校验。
- 报告/评审强化：项目评审报告/评审包（聚合校验报告 + 命令核对矩阵 + 项目摘要 + 交付清单 → zip/Markdown + PDF）。

### 改进

- 渲染备份测试消除秒级时序偶发失败。

## [4.7.0] - 2026-09-02

### 新增

- 4.7 部署运维与可观测。
- 更新兜底链（对齐 AL）：latest.yml 直查 + 安装包直接下载（Content-Length 完整性校验）+ Releases 页兜底 + 启动自动检查开关（checkUpdateOnStart）。
- 诊断中心：日志 / 审计 / 崩溃 / 性能 / 系统信息一处可查 + 一键导出支持包（zip）。
- 健康检查/自检：环境 / 引擎（AI Hub + Python）/ 网络 / 依赖，可读报告 + 导出 JSON。
- 本地遥测：本地化、脱敏、默认关闭、可配置、可导出（telemetry.jsonl）。

### 改进

- 审计日志：关键操作（项目新建/打开/保存/渲染/导出/删除）写入 audit.jsonl（脱敏）。
- 安装/升级体验：构建配置校验脚本 + 离线/无网络友好提示 + 完整性校验显示。

## [4.6.0] - 2026-09-02

### 新增

- 4.6 质量与测试体系。
- 测试覆盖率门禁：后端 pytest-cov（≥55%，实测 60.67%）+ 前端 vitest coverage；CI 覆盖率 job + 阈值常量单源 + 基线棘轮（只升不降）。
- 测试数据资产：样例项目/参数表/模板/渲染基线（清单 manifest + README，pytest/vitest 双端消费）。

### 改进

- 测试报告：聚合 pytest/vitest/golden/性能/校验门禁 → `reports/quality_report.json` + HTML。
- 质量仪表盘：「质量」标签（覆盖率/门禁/测试通过率/校验通过率/性能基准，本地聚合）。

## [4.5.0] - 2026-09-02

### 新增

- 4.5 数据准确性与校验。
- 一致性校验引擎（参数表完整性 / 模板与参数一致性 / 配置字段校验）。
- 导出数据核对（渲染产物与参数/模板一致性 + 漂移检测）。

### 改进

- IP 规划校验（掩码 / 子网重叠 / 网关冲突 / 越界 / 重复）。
- AI 规划器准确性校验（工具返回与实际状态核对）。
- 校验面板（一键校验 / 按严重度分组 / 定位 / 导出报告 JSON / 门禁提示）+ CI 校验门禁。

## [4.4.0] - 2026-09-01

### 新增

- 4.4 效率与自动化工作流。
- 统一快捷键规范（10 标准键：撤销/重做、命令面板、侧边栏、终端、新建、渲染、导出、项目面板、设置、Cheatsheet）+ Cheatsheet。
- 批量操作增强：批量渲染（并行/进度/失败汇总）、批量导出（多项目包）、批量编辑（参数批量应用）。

### 改进

- 一键管线「导入规划 → 渲染 → 导出」（步骤状态/可中断/重试）+ 模板批处理。
- 最近使用/收藏/模板入口（欢迎页「最近项目」「从模板新建」）。
- 命令面板命令全集（项目/模板/渲染/导出/批量/管线/最近收藏/设置/终端）。

## [4.3.0] - 2026-09-01

### 新增

- 4.3 AI 智能体验深化（不含 Hermes）。
- AI 项目/模板操作工具：list / create / update / delete / import / export / create_from_template / preview（8 工具，权限分级 + 校验 + 可读错误）。
- 批量 AI 操作命令化（终端 export/template/batch + 命令面板批量渲染/导出/项目/模板）。

### 改进

- 对话深化：摘要衔接「摘要后继续」、会话归档/取消归档、确认卡片可编辑工具参数（b64）。
- 技能/记忆深化：技能库 list/详情/启用禁用 + 持久化；记忆去抖/prompt 缓存失效测试锁定。
- AI 能力矩阵标准化（8 维度：工具/对话/流式/权限/自主/技能/记忆/规划器）+ 无 Hermes 审计。

## [4.2.0] - 2026-09-01

### 新增

- 4.2 稳定性与数据安全。
- 崩溃上报：本地转储 + 脱敏 errors.log（修复 Bearer/JWT 脱敏顺序）+ 渲染崩溃自动 reload + 未捕获异常兜底。
- 项目编辑自动保存（防抖 800ms + 关闭前 flush）+ 崩溃/关闭后恢复。

### 改进

- 编辑撤销命令栈（深拷贝快照 / 栈深 50 / Ctrl+Z+Y + 工具条，重启保留）。
- 项目版本回滚（`.mc_history/` 最近 10 版）+ 备份轮转（`.mc_backups/` 5 份）+ 一键恢复。
- 大项目性能：Web Worker 化大参数表数据准备 + 万行参数表虚拟化渲染 + 性能门禁场景 C（万行 ≤30s）。
- 性能仪表盘（操作耗时 / 内存 / 渲染长任务 / 基准对比）。

## [4.1.0] - 2026-09-01

### 新增

- 4.1 视觉与品牌统一。
- 主题系统 DTCG 化：light/dark/system/**high-contrast** 四主题，无闪变 + 持久化。
- 高对比主题（WCAG AA）：正文 21:1、次要 14.7:1、弱化 7.6:1、边界 4.8:1、主按钮 8.7:1。

### 改进

- 组件库视觉收敛到契约 token（Button/Input/Select/Modal/Table/Tabs/Popover/Toast/ContextMenu）。
- 品牌资产统一（启动画面/About/徽标/字体栈按契约）。
- 空态/加载/错误态统一（EmptyState/LoadingState/ErrorState）。

## [4.0.0] - 2026-08-29

### 新增

- 4.0 系列启动 · 工程基座与质量门禁。
- 引入 Playwright Electron E2E（冒烟三件套：启动→建项目→渲染→导出）。
- 新增 golden 基线、模板校验、性能门禁、渲染安全基线四道 CI 门禁（对齐 AL 工程化）。
- 版本单源 + 发布说明自动抽取（`sync-version --release-notes`）。

### 改进

- 设计 token 单源扩展（按双端 4.0 契约：语义色/中性/surface/圆角/阴影/间距/动效/字体）+ 断言单测防漂移。
- 组件行为契约（Modal/Popover/ContextMenu/Select/Toast）补齐（ESC 关闭/焦点管理/受控）。
- 工程对齐：ESLint `no-unused-vars` 升 error，lint 0 error 门禁。

## [3.10.7] - 2026-08-29

### 新增

- 会话工具栏上下文摘要 + 长会话截断提示（上下文压缩）。
- 打磨轮（4.0 前最终打磨）用户体验增强。

### 改进

- 长对话上下文自动压缩，保持 AI 会话稳定与响应质量。
- 项目/模板/渲染链路稳定性优化。

### 修复

- 修复多处 3.10 系列打磨中发现的问题（详见各分项提交说明）。

### 质量门禁

- 引入 4.0.0 工程基座前置：E2E（Playwright Electron）、golden 基线、模板校验、性能门禁、渲染安全基线、版本/发布说明单源（对齐 AL）。
