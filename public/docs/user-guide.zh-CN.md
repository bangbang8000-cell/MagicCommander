# MagicCommander 使用指南

## 简介

MagicCommander 是一款专业的网络设备配置管理工具，可以帮助您高效地管理网络设备配置文件，支持 Excel 参数导入、Jinja2 模板渲染、批量输出配置等功能。

---

## 快速开始

### 1. 创建新项目

1. 点击菜单 **文件 → 新建项目**
2. 输入项目名称
3. 点击确认创建

### 2. 配置参数

1. 在项目浏览器中打开 `para.xlsx` 文件
2. 填写设备参数信息（名称、IP、端口等）
3. 保存文件

### 3. 创建模板

1. 在 `templates` 文件夹中创建 Jinja2 模板文件
2. 使用 `{{变量名}}` 语法引用参数

### 4. 执行渲染

1. 切换到 **渲染操作** 面板
2. 选择要渲染的项目
3. 配置输出格式（配置/YAML，是否包含 SN）
4. 点击 **执行渲染**

---

## 界面介绍

### 活动栏（左侧图标）

| 图标 | 功能 | 快捷键 |
|------|------|--------|
| 搜索 | 全局搜索 | Ctrl+Shift+F |
| 资源管理器 | 项目文件浏览 | Ctrl+Shift+E |
| 渲染操作 | 执行渲染任务 | Ctrl+Shift+R |
| 标签打印 | 批量打印标签 | Ctrl+Shift+L |
| 输出结果 | 查看渲染输出 | Ctrl+Shift+O |

### 菜单栏

- **文件** - 项目管理、刷新
- **视图** - 切换面板、主题切换
- **帮助** - 使用指南、关于

---

## 核心功能

### 项目管理

- 创建新项目
- 打开/删除项目
- 在文件管理器中打开项目目录

### 参数配置

支持的 Excel 文件格式：
- `.xlsx` - Excel 2007+ 格式
- `.xls` - Excel 97-2003 格式

### 模板渲染

模板语法示例：

```jinja2
# 设备配置模板
system-view
device-name {{device_name}}
ip address {{ip_address}} {{subnet_mask}}
port {{port}}
```

支持的变量：
- `{{变量名}}` - 简单变量
- `{% for item in list %}...{% endfor %}` - 循环
- `{% if condition %}...{% endif %}` - 条件判断

### 输出类型

| 类型 | 说明 |
|------|------|
| 配置输出 | 标准配置文件 |
| SN配置 | 带序列号配置 |
| YAML输出 | YAML 格式配置 |
| YAML+SN | YAML 格式带序列号 |

---

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl+Shift+E | 打开项目浏览器 |
| Ctrl+Shift+F | 全局搜索 |
| Ctrl+Shift+R | 打开渲染操作 |
| Ctrl+Shift+L | 打开标签打印 |
| Ctrl+Shift+O | 打开输出结果 |
| Ctrl+B | 切换侧边栏 |
| F5 | 刷新页面 |

---

## 常见问题

### Q: 如何导入设备参数？
A: 在项目根目录编辑 `para.xlsx` 文件，每行代表一个设备，列代表不同参数。

### Q: 模板文件放在哪里？
A: 模板文件应放在项目的 `templates` 文件夹中。

### Q: 输出文件在哪里查看？
A: 渲染后的文件会保存在 `output`、`output-sn`、`yaml`、`yaml-sn` 等文件夹中，可在输出结果面板查看。

---

## 技术支持

如有问题或建议，请联系开发团队。
