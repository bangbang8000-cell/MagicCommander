// ============================================================
// Electron 相关类型（向后兼容）
// 核心类型已迁移至 src/types/
// ============================================================

import type { RenderProgressEvent, RenderError } from '@/types'

// 重新导出以保持向后兼容
export type { RenderProgressEvent, RenderError } from '@/types'
